
services:
  postgres:
    image: postgres:latest 
    container_name: TeachTrackContainer
    restart: always
    env_file:
      - postgres.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
    driver: local